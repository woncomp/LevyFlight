<Window x:Class="LevyFlight.LevyFlightWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:shell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             mc:Ignorable="d" Title="Levy Flight" ShowInTaskbar="False"
             WindowStartupLocation="CenterScreen"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource {x:Static shell:VsBrushes.ToolWindowBackgroundKey}}"
             Foreground="{DynamicResource {x:Static shell:VsBrushes.WindowTextKey}}"
             Loaded="Window_Loaded" SourceInitialized="Window_SourceInitialized" Closing="Window_Closing">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid>
        <!-- Host for the real quick-open UI, loaded lazily -->
        <ContentPresenter x:Name="contentHost" />

        <!-- Loading overlay -->
        <Grid Background="{DynamicResource {x:Static shell:VsBrushes.ToolWindowBackgroundKey}}"
               Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
               Panel.ZIndex="1000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="120" Height="4" Margin="0,0,0,8"/>
                <TextBlock Text="Loading..." HorizontalAlignment="Center" FontSize="12"
                           Foreground="{DynamicResource {x:Static shell:VsBrushes.WindowTextKey}}"/>
            </StackPanel>
        </Grid>

        <!-- Error overlay -->
        <Grid Background="{DynamicResource {x:Static shell:VsBrushes.ToolWindowBackgroundKey}}"
               Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"
               Panel.ZIndex="1001">
            <TextBlock Text="{Binding ErrorMessage}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontSize="12"
                       Foreground="{DynamicResource {x:Static shell:VsBrushes.WindowTextKey}}"/>
        </Grid>
    </Grid>
</Window>
